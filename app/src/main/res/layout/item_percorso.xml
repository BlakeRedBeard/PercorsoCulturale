<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:clickable="true"
    android:focusable="true"
    android:id="@+id/item_percorso"
    android:layout_weight="1"
    style="@style/cardboard">

    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/thumbnail_card"
        card_view:cardCornerRadius="15dp"
        card_view:cardElevation="0dp"
        android:layout_margin="0dp">
        <ImageView
            android:id="@+id/item_percorso_image"
            android:layout_alignParentStart="true"
            android:layout_alignParentEnd="true"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:adjustViewBounds="true"
            android:scaleType="centerCrop"
            />


    </androidx.cardview.widget.CardView>
    <TextView
        android:id="@+id/item_percorso_name"
        android:text="Lorem Ipsum Dolore Sit Amet"
        android:layout_alignBottom="@id/thumbnail_card"
        android:layout_alignStart="@id/thumbnail_card"
        android:layout_alignEnd="@+id/thumbnail_card"
        android:layout_height="wrap_content"
        android:layout_width="0dp"
        android:maxLines="3"
        android:background="@drawable/border"
        android:fontFamily="sans-serif-black"
        android:textStyle="italic"
        android:paddingStart="10dp"
        android:paddingEnd="20dp"
        android:textAllCaps="false"
        android:textColor="#EDE8E8"
        android:textSize="25dp"
        />

</RelativeLayout>
